        <form method="post" name="form" action="/5">
			<input type = "number" name="number" placeholder="Ex salário: 1526,45" min="0" step="0.01">
			<input type = "number" name="ajuste" placeholder="Ex percentual %: 25" min="0" step="0.01">
			<button type="submit">Enviar</button>
			<a href="/"><button type="button" id="back">Voltar</button></a>
		</form>
    </div>

    {{#if dados}}
        <div class="message message-empty" id="message">
            <div class="title">
                {{mensagemTitulo}}
                <button class="close" class="close"><i class='fa fa-times-circle'></i></button>
            </div>
            <div class="text" id="value"><p>{{mensagemTexto}}.</p></div>
        </div>
    {{/if}}
    {{#if dadosValidos}}
        <div class="message message-zero" id="message">
            <div class="title">
                {{mensagemTitulo}}
                <button class="close" class="close"><i class='fa fa-times-circle'></i></button>
            </div>
            <div class="text" id="value"><p>O valor do salário com ajuste é <span class='negrito'>R${{total}}</span>.</p></div>
        </div>
    {{/if}}
	
<script type="text/javascript">
	var i; 
	var close = document.getElementsByClassName("close");
	var message = document.getElementsByClassName("message");

	for(i = 0; i < close.length; i++){
		close[i].addEventListener("click", closeMessage);
	}

	function closeMessage(){
		var number = document.forms["form"]["number"];
		var ajuste = document.forms["form"]["ajuste"];
		number.value = "";
		ajuste.value = "";

		for(i = 0; i < message.length; i++){
			message[i].style.display = "none";
		}
	}
</script>